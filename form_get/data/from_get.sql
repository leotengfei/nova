CREATE TABLE form_get(
    fid INT PRIMARY KEY AUTO_INCREMENT,
    uname VARCHAR(10) NOT NULL DEFAULT '',
    utel VARCHAR(32) NOT NULL DEFAULT '',
    uschool VARCHAR(100) NOT NULL DEFAULT '',
    ugrade VARCHAR(32) NOT NULL DEFAULT ''
)